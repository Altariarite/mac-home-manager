# Move .direnv out of the project so tools like shopify-cli don't
# pick up the cached copy of the flake source in `.direnv/flake-inputs/`.
direnv_layout_dir() {
  echo "${XDG_CACHE_HOME:-$HOME/.cache}/direnv/layouts/${PWD//\//-}"
}

use flake
